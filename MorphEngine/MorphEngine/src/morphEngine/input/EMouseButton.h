#pragma once

#include "utility/MString.h"
#include "utility\MEnum.h"

namespace morphEngine
{
	namespace input
	{
		DECLARE_MENUM(EMouseButton, LEFT, MIDDLE, RIGHT);
		DECLARE_MENUM(EMouseAxis, HORIZONTAL, VERTICAL);
	}
}